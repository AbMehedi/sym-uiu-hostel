when@dev:
    web_profiler:
        toolbar: false   # Disabled to avoid extra /_wdt/ round-trip per page. Enable when debugging.

    framework:
        profiler:
            collect_serializer_data: true

when@test:
    framework:
        profiler:
            collect: false
            collect_serializer_data: true
